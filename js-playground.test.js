import { describe, expect, it } from "vitest";

const main = () => {
};
describe("Duplicate Arguments", function () {
  it("test", () => {
    expect(main()).toEqual();
  });
});

//ReadMe to get this to work install vitest 
// running -> npm run test <- will run the code locally